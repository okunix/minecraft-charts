apiVersion: v1
kind: Service
metadata:
  name: {{ include "minecraft.serviceName" . }}
  labels:
    {{- include "minecraft.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ include "minecraft.fullname" . }}
  type: LoadBalancer 
  ports:
    - port: {{ .Values.minecraft.port }} 
      targetPort: minecraft
      name: minecraft
      protocol: TCP
